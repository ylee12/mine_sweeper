
# Use this when creating a new repository on the command line

echo "# Battle_ships" >> README.md
git init
git add README.md
git commit -m "first commit"
git remote add origin git@github.com:ylee12/Battle_ships.git
git push -u origin master


===========================================

# Use this when pushing an existing repository from the command line
git remote add origin git@github.com:ylee12/Battle_ships.git
git push -u origin master

